{
  "name": "@panda-project/core",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tspc --watch --emitDeclarationOnly",
    "build": "rm -rf dist && tspc",
    "type-check": "tspc --noEmit",
    "jest": "jest"
  },
  "dependencies": {
    "@panda-project/config": "workspace:*"
  },
  "private": true
}
